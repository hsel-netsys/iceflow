application = {
    applicationName: tstr,
    nodes: [ * node ],
}

node = {
    task: tstr,
    name: tstr,
    description: tstr,
    executor: "container" / "static",
    container: {
        image: tstr,
        resources: {
            cpu: uint,
            memory: uint,
            taskComplexity: uint,
        }
    },
    communicationTask: {
        ? payloadSize: uint,
    },
    ? downstream: [ * tstr ],
}
