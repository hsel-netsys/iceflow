syncPrefix: /VP
nodePrefix: ageDetection

consumer:
  topic: /faceDetection/dataMain
  inputThreshold: 200 # TODO: This is unused at the moment
  partitions: [0]

producer:
  topic: /ageDetection/dataMain
  publishInterval: 0
  numberOfPartitions: 1

measurements:
  saveThreshold: 100
